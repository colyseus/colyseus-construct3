{
  "name": "colyseus-construct3",
  "version": "0.0.0",
  "description": "Colyseus Multiplayer Client Plugin for Construct3.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server plugin -p 5432 --cors",
    "chrome": "open -a Google\\ Chrome --args https://editor.construct.net '--disable-web-security' '--disable-extensions' '--disable-plugins'",
    "build": "pushd plugin && zip -r ../colyseus.c3addon . -x \"*.DS_Store\" && popd"
  },
  "author": "Endel Dreyer",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.11.1"
  }
}
